---
console.time("Test");

const response = await fetch(
  "http://localhost/astro-wp/wp-json/wp/v2/pages?slug=home",
);

const data = await response.json();

console.timeEnd("Test");
---

<h1>{data[0].title.rendered}</h1>
